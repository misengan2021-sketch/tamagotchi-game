name: Android CI (debug)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Unzip project
        run: |
          unzip tamagotchi_full_project.zip -d project
          ls project

      # Патчим settings.gradle в GROOVY-синтаксисе:
      # 1) pluginManagement с версиями плагинов
      # 2) dependencyResolutionManagement с репозиториями google() и mavenCentral()
      - name: Patch settings.gradle (groovy)
        run: |
          cat > project/settings.gradle << '

