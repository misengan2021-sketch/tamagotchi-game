name: Android CI (debug)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Unzip project
        run: |
          unzip tamagotchi_full_project.zip -d project
          ls project

      # Удаляем строку с kotlinCompilerExtensionVersion из app/build.gradle
      - name: Patch app build.gradle (remove bad compose compiler version)
        run: |
          cd project/app
          echo "===== app/build.gradle BEFORE ====="
          cat build.gradle
          sed -i '/kotlinCompilerExtensionVersion/d' build.gradle
          echo "===== app/build.gradle AFTER ====="
          cat build.gradle

      # settings.gradle с плагинами и репозиториями
      - name: Patch settings.gradle
        run: |
          cd project
          cat <<EOF > settings.gradle
          pluginManagement {
              repositories {
                  google()
                  mavenCentral()
                  gradlePluginPortal()
              }
              plugins {
                  id "com.android.application" version "8.5.0"
                  id "org.jetbrains.kotlin.android" version "1.9.0"
              }
          }

          dependencyResolutionManagement {
              repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
              repositories {
                  google()
                  mavenCentral()
              }
          }

          rootProject.name = "TamagotchiFull"
          include ":app"
          EOF
          echo "===== settings.gradle ====="
          cat settings.gradle
          echo "==========================="

      # gradle.properties с включённым AndroidX
      - name: Create gradle.properties
        run: |
          cd project
          cat <<EOF > gradle.properties
          android.useAndroidX=true
          android.enableJetifier=true
          EOF
          echo "===== gradle.properties ====="
          cat gradle.properties
          echo "============================="

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Gradle
        run: |
          wget https://services.gradle.org/distributions/gradle-8.7-bin.zip
          unzip -q gradle-8.7-bin.zip -d $HOME/gradle
          echo "$HOME/gradle/gradle-8.7/bin" >> $GITHUB_PATH
          gradle -v

      - name: Build debug APK
        run: |
          cd project
          ls
          gradle :app:assembleDebug

      - name: Upload debug APK
        uses: actions/upload-artifact@v4
        with:
          name: app-debug-apk
          path: project/app/build/outputs/apk/debug/*.apk
